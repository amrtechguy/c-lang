#include <stdio.h>

/*
    THe App Objective:
        -> Takes a list of numbers as arguments.
        -> Store them in the heap with max size of 100 int.
        -> Get the average and print the result to screen.
        -> Free the memory. 
*/

int main(int argc, char **argv) {
    
    // loop on the arguments

    // convert each argument to int

    // allocate a memory in the heap for the ints

    // free the allocated memory

    return 0;
}