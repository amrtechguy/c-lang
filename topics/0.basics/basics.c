// this is how to include a c library's header in my program.
#include <stdio.h>

/*
    the c program must contain a function named main,
    the program execution starts from it.
*/
int main() {
    // the return number 0 means that the program executed well and didn't fail.
    return 0;
}